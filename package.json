{
  "name": "mini-dhawan-portfolio",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "deploy:pages": "astro build && wrangler pages deploy dist --project-name minidhawan",
    "astro": "astro"
  },
  "dependencies": {
    "astro": "^5.7.0"
  }
}
